[project]
name = "pomowatch"
version = "0.1.0"
description = "A simple Pomodoro timer desktop app built with Tkinter"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "numpy>=1.24",
    "sounddevice>=0.4",
    "ttkthemes>=3.2",
]

[tool.uv]
package = false
