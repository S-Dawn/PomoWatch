[project]
name = "pomowatch"
version = "0.1.0"
description = "A simple Pomodoro timer desktop app built with Tkinter"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[project.scripts]
pomowatch = "main:main"

[tool.uv]
package = false
